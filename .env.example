# Shared defaults for the monorepo.
# Copy to .env at repo root.
# Do not put secrets in this file.

# Required: webhook-proxy HTTP port
WEBHOOK_PROXY_PORT=4001

# Required: kitchen-relay HTTP port (used for GET /health)
KITCHEN_RELAY_PORT=4000

# Required: kitchen-relay printer adapter (`console` or `lp`)
KITCHEN_PRINTER_ADAPTER=console

# Optional: printing relay → webhook proxy base URL (default http://127.0.0.1:4001)
# KITCHEN_WEBHOOK_PROXY_URL=http://127.0.0.1:4001

# Optional: append printed tickets to this file
# KITCHEN_PRINT_LOG=./kitchen-print.log

# Optional: CUPS queue name when KITCHEN_PRINTER_ADAPTER=lp
# KITCHEN_PRINTER_NAME=YourKitchenPrinter

# Optional: lp job title when KITCHEN_PRINTER_ADAPTER=lp
# KITCHEN_PRINTER_TITLE=RicosKitchen

# Optional retry behavior for printing
# KITCHEN_PRINT_MAX_ATTEMPTS=5
# KITCHEN_PRINT_RETRY_INITIAL_DELAY_MS=200

# Optional: append failed tickets after retries
# KITCHEN_PRINT_DEAD_LETTER=./kitchen-print-dead-letter.log

# Optional: path for processed Stripe event ids
# KITCHEN_IDEMPOTENCY_STORE=
