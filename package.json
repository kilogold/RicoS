{
  "name": "ricos",
  "private": true,
  "workspaces": [
    "web",
    "kitchen-relay",
    "packages/shared"
  ],
  "engines": {
    "bun": ">=1.2.0"
  },
  "scripts": {
    "preinstall": "bun scripts/enforce-bun-install.mjs",
    "dev:web": "bun --env-file=.env --env-file=.env.local run --filter './web' dev",
    "dev:kitchen": "bun --env-file=.env --env-file=.env.local run --filter './kitchen-relay' dev",
    "build": "bun --env-file=.env --env-file=.env.local run --filter './web' build",
    "lint": "bun --env-file=.env --env-file=.env.local run --filter './web' lint"
  },
  "packageManager": "bun@1.2.0"
}
